import mongoose from 'mongoose'

export const database = async (): Promise<void> => {
    try {
        await mongoose.connect('mongodb://localhost:27017/pluggskap_province_db')
        console.error('MongoDB successfully connected')
    } catch (error) {
        console.error('MongoDB connection error', error);
        process.exit(1);
    }
};

Från header. Kod som krockade med inline i css. Resultat: Meny visades inte på stor skärm.
    // function toggleMenu(menuId) {
    //   const meny = document.getElementById(menuId);
    //   if (meny.style.display === "none") {
    //     meny.style.display = "block";
    //   } else {
    //     meny.style.display = "none";
    //   }
    // }

    // button.addEventListener("click", () => {
    //   toggleMenu("menu");
    // });